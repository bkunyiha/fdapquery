[package]
name         = "fdapquery-logical-plan"
description  = "LogicalPlan, LogicalExpr, DataFrame."
edition.workspace      = true
license.workspace      = true
version.workspace      = true
authors.workspace      = true
rust-version.workspace = true

[dependencies]
arrow-schema = { workspace = true }
chrono       = { workspace = true }      # NaiveDate for `LiteralDate`
fdapquery-datatypes    = { path = "../fdapquery-datatypes" }
fdapquery-datasource   = { path = "../fdapquery-datasource" }
